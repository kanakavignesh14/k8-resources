apiVersion: v1
kind: Service
metadata: 
   name: nginx  # service name


spec:
  selector:
     project: roboshop
     component: frontend
     environment: dev
  ports:
     - protocol: TCP
       port: 80 # sevice port
       targetPort: 80 # container port   forwart req to another container